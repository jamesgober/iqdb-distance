# iqdb-distance v0.1.0 — Scaffold

**The repository bootstrap.** v0.1.0 establishes the structure, tooling, and quality gates the distance layer is built on — no domain logic yet. It exists so the implementation lands one verified step at a time onto a foundation that already enforces the iQDB engineering standard.

## What is iqdb-distance?

The innermost loop of the iQDB vector database. Every search computes thousands of distances, so this crate owns the math — cosine, dot product, Euclidean, Manhattan, and Hamming — and optimizes it aggressively while keeping a readable scalar reference. It sits one level above `iqdb-types` and is depended on by `iqdb-quantize` and every index crate (`iqdb-flat`, `iqdb-hnsw`, `iqdb-ivf`).

## What's in 0.1.0

- `Cargo.toml` with crate metadata, the Rust 2024 edition, and MSRV `1.87`.
- Dual `Apache-2.0 OR MIT` license files.
- `README.md`, `CHANGELOG.md`, and a documentation skeleton (`docs/API.md`).
- `REPS.md` compliance baseline and the shared lint config (`clippy.toml`, `rustfmt.toml`, `deny.toml`).
- `.github/workflows/ci.yml`: the CI matrix across Linux, macOS, and Windows on stable and the 1.87 MSRV, plus the loom and security (`cargo audit` + `cargo deny`) jobs.
- `dev/DIRECTIVES.md` and `dev/ROADMAP.md`: the committed engineering standards and the phase-by-phase plan to 1.0.

## Breaking changes

**None** — this is the first tag.

## Verification

The scaffold compiles clean and the placeholder smoke test passes across the CI matrix:

```bash
cargo fmt --all -- --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test --all-features
RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --all-features
```

MSRV: Rust 1.87.

## What's next

- **v0.2.0 — scalar reference metrics + the `Distance` trait.** The hard part, not deferred: every metric with a readable scalar implementation and the type-level trait, property-tested for its core invariants.
- **v0.3.0 — SIMD.** AVX2 and NEON kernels with runtime dispatch, differentially tested against the scalar ground truth.

## Installation

```toml
[dependencies]
iqdb-distance = "0.1"
```

## Documentation

- [README](https://github.com/jamesgober/iqdb-distance/blob/main/README.md)
- [API reference](https://github.com/jamesgober/iqdb-distance/blob/main/docs/API.md)
- [ROADMAP](https://github.com/jamesgober/iqdb-distance/blob/main/dev/ROADMAP.md)
- [Standards (REPS)](https://github.com/jamesgober/iqdb-distance/blob/main/REPS.md)
- [CHANGELOG](https://github.com/jamesgober/iqdb-distance/blob/main/CHANGELOG.md)

---

**Changelog:** [`CHANGELOG.md`](https://github.com/jamesgober/iqdb-distance/blob/main/CHANGELOG.md).
